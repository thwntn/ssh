Install

--sudo apt-get install openssh-server

Connect

--ssh 'username'@'ip'

Create:keygen

--ssh-keygen

Copy:key:to:server

--cat ~/.ssh/id_rsa.pub | ssh 'username'@'ip' 'cat >> ~/.ssh/authorized_keys'
